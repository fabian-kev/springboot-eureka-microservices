server:
  port: 5000
spring:
  application:
    name: gateway-service
  profiles:
    include: router
  #    active:

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
